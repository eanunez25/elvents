<%= provide(:title, "Create event") %>
<h1>Create Event</h1>

<%= form_with(model: @event, local:true) do |f| %>

  <%= f.label :name %>
  <%= f.text_field :name %>

  <%= f.submit "Create event"%>
  
<% end %>

